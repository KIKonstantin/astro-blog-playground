---
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
import BaseLayout from "../layouts/BaseLayout.astro";
const pageTitle = "About me";
const identity = {
	firstName: "Konstantin",
	lastName: "Kostadinov",
	country: "Bulgaria",
	occupation: "Front-end head developer",
	hobbies: ["reading", "chess", "traveling", "photography"],
};
const skills = [
	"HTML",
	"CSS/SASS",
	"JS",
	"D3",
	"Vue",
	"React",
	"Node",
	"Astro",
	"Writing Docs",
];
const happy = true;
const finished = false;
const goal = 3;
---

<BaseLayout pageTitle={pageTitle}>
	<p>A few facts for me:</p>
	<ul>
		<li>My name is {identity.firstName}</li>
		<p>
			I live in {identity.country} and I work as {identity.occupation}
		</p>
		{
			identity.hobbies.length >= 2 && (
				<li>
					Tow of my hobbies are: {identity.hobbies[0]} and{" "}
					{identity.hobbies[1]}
				</li>
			)
		}
	</ul>
	<p>My skills are:</p>
	<ul>
		{skills.map((skill) => <li class="skill">{skill}</li>)}
	</ul>
	{happy && <p>I am happy to be learning Astro!</p>}
	{finished && <p>I finished this tutorial!</p>}
	{
		goal === 3 ? (
			<p>My goal is to finish in 3 days.</p>
		) : (
			<p>My goal is not 3 days.</p>
		)
	}
</BaseLayout>
